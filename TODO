x (A) str.h: initial implementation +string @container
(A) str.h: append and formatting +string @container
(A) str.h: parsing and splitting +string @container
(A) str.h: trim, strip, format +string @container
(A) str.h: contains, suffixes, prefixes +string @container
(A) str.h: indexing, replacing and iterating +string @container
x (A) vect.h: initial implementation +vect @container
x (A) vect.h: contains +vect @container
(A) vect.h: iterator +vect @container
(B) sync.h: initial spinlock +sync @lock
(B) sync.h: initial mutex +sync @lock
(B) sync.h: initial atomics +sync @lock
(B) chan.h: initial implementation +sync @chan
(C) str.h: full test coverage +string @container
(C) vect.h: proper automated testing +vect @container
